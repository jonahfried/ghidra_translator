{
  param_1 : array_ptr_to_long,
  local_10 : result_value,
}
Reasoning:
- `param_1` is a pointer to a long array, so `param_1` is inferred to be `array_ptr_to_long`.
- `local_10` seems to store the result of the function, so `local_10` is inferred to be `result_value`.